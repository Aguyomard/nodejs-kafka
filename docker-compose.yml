services:
  app:
    build: .
    ports:
      - '3000:3000'
      - '9229:9229'
    volumes:
      - .:/app
      # - /app/node_modules
    env_file:
      - .env
    command:
      [
        'nodemon',
        '--inspect=0.0.0.0:9229',
        '--ext',
        'ts',
        '--exec',
        'tsx',
        'src/server.ts',
      ]

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '5173:5173'
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: pnpm dev
